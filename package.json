{
  "name": "JATE",
  "version": "1.0.0",
  "description": "",
  "main": "server.js",
  "scripts": {
    "start": "npm install && cd server && npm install && cd ../client && npm install && concurrently \"npm run build --prefix client\" \"npm run start --prefix server\""
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.17.1",
    "if-env": "^1.0.4",
    "concurrently": "^5.2.0",
    "nodemon": "^2.0.4"
  }
}
